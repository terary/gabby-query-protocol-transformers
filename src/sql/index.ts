import { Where } from "./predicates/where-functions";
import { Select } from "./projection/select-functions";
import { OrderBy } from "./projection/orderby-functions";
export const SQL = {
  Select,
  Where,
  OrderBy,
};
